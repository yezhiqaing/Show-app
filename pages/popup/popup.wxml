<van-search
  value="{{ value }}"
  placeholder="请输入搜索关键词"
  show-action
  bind:search="onSearch"
  bind:cancel="onCancel"
/>
<HistroyHot wx:if="{{blockShow===1}}"></HistroyHot>
<ProductList wx:elif="{{blockShow===2}}"></ProductList>
<TipsList wx:else></TipsList>
